<h2 id="header">New Game</h2>

<div id="form_name", class="hidden"><%= "game" %></div>

<%= form_with(model: @game, local: true) do |form| %>

  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>
      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<body class="container">
		<br>
		<%= render partial: "editfields", locals: {form: form} %>
		<br>

		<div class="hidden">
			<%= render partial: "calcfields", locals: {form: form} %>
		</div>
				
		<div class="row">
		  <div class="col-sm-2" >
		    <%= form.submit "Save", class:"btn-lg btn-primary", id:"Save"%>
		  </div>
		  <h3 class="col-sm-1"><%= link_to 'Back', games_path%></h3>
		</div>

	</body>

<% end %>


